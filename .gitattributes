# Media used to be tracked with Git LFS. Uploads made through the CMS (GitHub API)
# cannot go through LFS, so new files are stored as regular git objects.
# Existing LFS-tracked files keep working; they are simply no longer matched here.
